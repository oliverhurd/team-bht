VITE_CLERK_PUBLISHABLE_KEY=pk_test_replace_with_your_key
# Clerk secret key (server-side only). Do NOT commit real secret keys to the repo.
CLERK_SECRET_KEY=sk_test_replace_with_your_secret

# Notes:
# - For local dev with Vite, set `VITE_CLERK_PUBLISHABLE_KEY` to your publishable key.
# - The frontend only needs the publishable key. Keep `CLERK_SECRET_KEY` on the server.
# - Your provided Next.js env name (`NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`) will also be read
#   by the app if present, but Vite exposes only `VITE_`-prefixed env vars to the client.